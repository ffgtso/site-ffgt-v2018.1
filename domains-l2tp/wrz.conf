-- This is a site configuration for Gluon v2018.1
--
{
  domain_names = {
    wrz = 'Freifunk Müritz'
  },

  domain_director = {
    url = 'http://setup.ipv6.4830.org/dd.php',
    switch_after_offline = 360,
    enabled = true,
  },

  -- 32 bytes of random data, encoded in hexadecimal
  -- This data must be unique among all sites and domains!
  -- Can be generated using: echo $(hexdump -v -n 32 -e '1/1 "%02x"' </dev/urandom)
  -- Generated by »echo "Freifunk Müritz" | awk 'BEGIN{for(n=0; n<256; n++) ord[sprintf("%c",n)]=n;} {newstr=""; for(i=1; i<33; i++) {if(i>length($0)) c="\0"; else c=substr($0, i, 1); newstr=sprintf("%s%02x", newstr, ord[c]);} printf("%-64.64s", newstr)}' ; echo«
  domain_seed = '4672656966756e6b204dfc7269747a0000000000000000000000000000000000',

  -- Prefixes used within the mesh.
  -- prefix6 is required, prefix4 can be omitted if next_node.ip4
  -- is not set.
  prefix6 = 'fd39:e4e3:eee1::/64',

  -- List of NTP servers in your community.
  -- Must be reachable using IPv6!
  ntp_servers = {'ntp.4830.org', 'ntp.services.ffgt.net'},

  -- Wireless configuration for 2.4 GHz interfaces.
  wifi24 = {
    -- Wireless channel.
    channel = 5,

    -- ESSID used for client network.
    ap = {
      ssid = 'l2tp.mueritz.freifunk.net',
      -- disabled = true, -- (optional)
    },

    mesh = {
      id = '12:23:de:ca:fb:ad',
      mcast_rate = 12000,
      -- disabled = true, (optional)
    },

    -- ibss = {
    --   ssid = '00:23:de:ca:fb:ad',
    --   bssid = '00:23:de:ca:fb:ad',
    --   mcast_rate = 12000,
    --   -- disabled = true, (optional)
    -- },
  },

  -- Wireless configuration for 5 GHz interfaces.
  -- This should be equal to the 2.4 GHz variant, except
  -- for channel.
  wifi5 = {
    channel = 44,
    ap = {
     ssid = 'l2tp.mueritz.freifunk.net',
      -- disabled = true, -- (optional)
    },

    mesh = {
      id = '12:23:de:ca:fb:ad',
      mcast_rate = 12000,
      -- disabled = true, (optional)
    },

    -- ibss = {
    --   ssid = '00:23:de:ca:fb:ad',
    --   bssid = '00:23:de:ca:fb:ad',
    --   mcast_rate = 12000,
    --   disabled = true,
    -- },
  },

  mesh = {
    -- Options specific to the batman-adv routing protocol (optional)
    -- batman_adv = {
      -- Gateway selection class (optional)
      -- The default class 20 is based on the link quality (TQ) only,
      -- class 1 is calculated from both the TQ and the announced bandwidth
      -- gw_sel_class = 1,
    -- },
    vxlan = false,
  },

  -- The next node feature allows clients to always reach the node it is
  -- connected to using a known IP address.
  next_node = {
    ip6 = 'fd39:e4e3:eee1::1',
  },

  mesh_vpn = {
    -- 1364 taken from Freifunk Muenster
    mtu = 1364,

	tunneldigger = {
	  brokers = {
	    'l2tp-ber01.4830.org:10005',
		'l2tp-gut01.4830.org:10005',
		'l2tp-gut02.4830.org:10005',
		'l2tp-ham01.4830.org:10005',
		'l2tp-ham02.4830.org:10005',
		'l2tp-fra01.4830.org:10005',
		'l2tp-ams01.4830.org:10005',
	  },
    },
  },
}
